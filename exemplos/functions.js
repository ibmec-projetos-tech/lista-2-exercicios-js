// Funções - Método tradicional

function soma(a, b) {
    return a + b;
}

console.log(soma(2, 3));

// Funções - Arrow functions

 const saudacao = (nome) => {
    return `Bem-vinda, ${nome}!`;
 }

 console.log(saudacao('Isabella'));
 console.log(typeof(saudacao));